name: Integrate

on:
  push:
    branches:
      - main

concurrency:
  cancel-in-progress: false
  group: '${{ github.workflow }}-${{ github.ref_name }}'

jobs:
  version:
    name: Version
    uses: cupel-co/workflows/.github/workflows/version.generate.yml@3f6e3882754ef9f709b10936e6792212b92ccce2
    permissions:
      contents: read

  release:
    name: Release
    uses: cupel-co/workflows/.github/workflows/release.create.yml@3f6e3882754ef9f709b10936e6792212b92ccce2
    needs:
      - version
    permissions:
      contents: write
    with:
      tag: "v${{ needs.version.outputs.sem-ver }}"
    secrets:
      google-chat-webhook-url: '${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}'
      gpg-private-key: "${{ secrets.GH_GPG_PRIVATE_KEY }}"
      gpg-private-key-password: "${{ secrets.GH_GPG_PRIVATE_KEY_PASSWORD }}"
      token: "${{ secrets.GH_RELEASE_PAT }}"
