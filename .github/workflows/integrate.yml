name: Integrate

on:
  push:
    branches:
      - main

concurrency:
  cancel-in-progress: false
  group: ${{ github.workflow }}-${{ github.ref_name }}

jobs:
  version:
    name: Version
    uses: cupel-co/workflows/.github/workflows/version.generate.yml@e126a14c220d8daa8d84e59431446dbb18a24e7d

  release:
    name: Release
    uses: cupel-co/workflows/.github/workflows/release.create.yml@e126a14c220d8daa8d84e59431446dbb18a24e7d
    needs:
      - version
    permissions:
      contents: write
    with:
      tag: "v${{ needs.version.outputs.sem-ver }}"
    secrets:
      google-chat-webhook-url: '${{ secrets.GOOGLE_CHAT_WEBHOOK_URL }}'
      gpg-private-key: "${{ secrets.GH_GPG_PRIVATE_KEY }}"
      gpg-private-key-password: "${{ secrets.GH_GPG_PRIVATE_KEY_PASSWORD }}"
      token: "${{ secrets.GH_RELEASE_PAT }}"