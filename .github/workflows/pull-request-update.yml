name: Update PR

on:
  pull_request:
    types:
      - opened
      - reopened
      - edited

concurrency:
  cancel-in-progress: false
  group: '${{ github.workflow }}-${{ github.ref_name }}'

jobs:
  pull-request:
    name: Pull Request
    uses: cupel-co/workflows/.github/workflows/pull-request.update.yml@ec59454b906ec3d2abdce35b1d83a1891ca618bf
    permissions:
      issues: read
      pull-requests: write
      repository-projects: read
    with:
      environment: Preview
